<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Biathlon_Esthivaukan.MainPage">

    <Grid>
        <!-- Row definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- Main content row, adjust height as needed -->
            <RowDefinition Height="Auto" />
            <!-- Bottom row for buttons -->
        </Grid.RowDefinitions>

        <!-- StackLayout for top buttons (400m, 200m, 600m) -->
        <StackLayout Grid.Row="0"
                     Spacing="10" VerticalOptions="Center" HorizontalOptions="Center">
            <!-- Buttons for 400m, 200m, 600m -->
            <Button x:Name="Btn400m"
                    Text="400 mètres"
                    Clicked="On400mClicked"
                    HorizontalOptions="Fill" />
            <Button x:Name="Btn200m"
                    Text="200 mètres"
                    Clicked="On200mClicked"
                    HorizontalOptions="Fill" />
            <Button x:Name="Btn600m"
                    Text="600 mètres"
                    Clicked="On600mClicked"
                    HorizontalOptions="Fill" />
        </StackLayout>

        <!-- Main content (if any) -->
        <VerticalStackLayout Grid.Row="0"
                             Padding="30,0"
                             Spacing="25">
            <!-- Add your main content here -->
        </VerticalStackLayout>

        <!-- Bottom buttons -->
        <Grid Grid.Row="1"
              ColumnDefinitions="*, *, *">

            <!-- Left button (Suiviperf) -->
            <Button x:Name="Suiviperf"
                    Text="Suiviperf"
                    WidthRequest="120"
                    HeightRequest="100"
                    BackgroundColor="Green"
                    TextColor="Black"
                    HorizontalOptions="Center"
                    VerticalOptions="End"
                    Grid.Column="0" />

            <!-- Center button (Course) -->
            <Button x:Name="Run_btn"
                    Text="Course"
                    Clicked="OnRunClicked"
                    WidthRequest="120"
                    HeightRequest="100"
                    BackgroundColor="Green"
                    TextColor="LightBlue"
                    HorizontalOptions="Center"
                    VerticalOptions="End"
                    Grid.Column="1" />

            <!-- Right button (Profil) -->
            <Button x:Name="Profil"
                    Text="Profil"
                    WidthRequest="120"
                    HeightRequest="100"
                    BackgroundColor="Green"
                    TextColor="Black"
                    HorizontalOptions="Center"
                    VerticalOptions="End"
                    Grid.Column="2" />
        </Grid>

        <!-- Circle "Go" centered in the middle of the bottom half -->
        <VerticalStackLayout>
            <Button x:Name="GoButton"
                    Text="Go"
                    WidthRequest="150"
                    HeightRequest="150"
                    BackgroundColor="Blue"
                    TextColor="White"
                    CornerRadius="75"
                    BorderColor="White"
                    BorderWidth="2"
                    Margin="0,400,0,0"
                    />
        </VerticalStackLayout>
    </Grid>

</ContentPage>
